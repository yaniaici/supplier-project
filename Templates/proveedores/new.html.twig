{% extends 'base.html.twig' %}
{% block title %}Nuevo proveedor{% endblock %}
{% block body %}
    <div class="container">
        <h1>{{title}}</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>

{% endblock %}